# Exercícios: Aula 09 – Módulos e Organização Profissional 📁

### 🟢 Nível: Básico
1.  **Export/Import**: Crie um módulo para uma classe `Calculadora` e importe-o em outro arquivo para realizar uma soma.
2.  **Export Default**: Exporte uma constante por padrão e importe-a com um apelido à sua escolha.

### 🟡 Nível: Intermediário
3.  **Barrel Files**: Crie uma estrutura de pastas para `controllers` e crie um arquivo `index.ts` que exporta tudo o que há dentro dela.
4.  **Namespaces**: Crie um `namespace` para agrupar funções de validação de formulário (e-mail, senha).

### 🔴 Nível: Desafio
5.  **Path Aliases**: Configure um alias `@models` no `tsconfig.json` e use-o para importar uma interface de uma pasta profunda do projeto.