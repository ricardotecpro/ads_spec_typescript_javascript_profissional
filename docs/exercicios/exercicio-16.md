# Exercícios: Aula 16 – Projeto Final e Deploy 🚀

### 🟢 Nível: Básico
1.  **Build de Produção**: Execute o comando `npm run build` e analise o código gerado na pasta `dist`.
2.  **Scripts NPM**: Configure um script `start` que execute o arquivo `dist/index.js`.

### 🟡 Nível: Intermediário
3.  **Docker Básico**: Escreva um `Dockerfile` que copie apenas o necessário para rodar a aplicação já compilada.
4.  **Variáveis de Ambiente**: Configure o seu projeto para ler a porta do servidor de uma variável de ambiente (`process.env.PORT`).

### 🔴 Nível: Desafio
5.  **Pipeline CI/CD**: Documente os passos necessários para configurar o deploy automático do seu projeto no GitHub Pages ou Vercel.